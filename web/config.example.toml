# environment variables first, then config file

is_debug = true

[db]
host = "localhost"
port = 3306
name = "dbname"
user = "user"
password = "password"

[redis]
host = "localhost"
port = 6379
password = "password"
db = 0
